import { cinzel } from "./fonts";

const Header = () => {
  return (
    <header>
      <h1 className={`${cinzel.className} text-6xl`}>BLACKEYE VISUALS</h1>
    </header>
  );
};

export default Header;
